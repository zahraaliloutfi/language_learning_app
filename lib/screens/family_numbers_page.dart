import 'package:flutter/material.dart';

class FamilyNumbers extends StatelessWidget {
  const FamilyNumbers({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}